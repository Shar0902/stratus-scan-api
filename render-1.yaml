services:
  - type: web
    name: stratus-scan-api
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn scan_api:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: HIBP_API_KEY
        sync: false
